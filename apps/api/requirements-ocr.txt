# CPU-only OCR profile. Keeping the PyTorch CPU index explicit prevents an
# ARM production host from resolving the multi-gigabyte CUDA dependency set.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu
torchvision==0.28.0+cpu
easyocr==1.7.2
