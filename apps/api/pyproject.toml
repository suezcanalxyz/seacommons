[project]
name = "suezcanal"
version = "1.0.0"
description = "SuezCanal.xyz — Open-source maritime SAR and threat awareness platform"
readme = "README.md"
license = { text = "AGPL-3.0-or-later" }
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.29",
    "pydantic>=2.7",
    "pydantic-settings>=2.2",
    "sqlalchemy[asyncio]>=2.0",
    "alembic>=1.13",
    "aiosqlite>=0.20",
    "httpx>=0.27",
    "websockets>=12.0",
    "redis>=5.0",
    "pynacl>=1.5",
    "blake3>=0.3",
    "click>=8.1",
    "rich>=13.7",
    "python-dateutil>=2.9",
    "python-dotenv>=1.0",
    "requests>=2.31",
    "PyJWT[crypto]>=2.8",
    "python-multipart>=0.0.9",
    "minio>=7.2",
    "prometheus-client>=0.21",
    "copernicusmarine>=2.3.0",
    "xarray>=2025.0.0",
    "opendrift>=1.14.9",
    "Pillow>=10.0",
    "apscheduler>=3.10",
    "psycopg[binary]>=3.2",
    "twifork>=2.3.5",
]

[project.optional-dependencies]
sdr = ["pyrtlsdr>=0.3"]
ocr = ["easyocr==1.7.2"]
tid = [
    "missile-tid @ git+https://github.com/tylerni7/missile-tid.git",
    "cartopy>=0.22",
    "shapely>=2.0",
    "pycurl>=7.45",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "ruff>=0.4",
    "mypy>=1.10",
    "jsonschema>=4.23",
    "pip-audit>=2.7",
]
full = ["suezcanal[sdr,ocr,tid,dev]"]

[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["core*", "sdk*", "edge*", "connect*"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Blocking critical-correctness baseline. The broader E/F cleanup remains
# visible in CI and can be tightened incrementally.
select = ["E9", "F63", "F7", "F82"]

[tool.mypy]
python_version = "3.11"
ignore_missing_imports = true
